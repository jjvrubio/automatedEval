[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "md2docx"
version = "0.1.0"
description = "CLI para convertir Markdown a DOCX con Pandoc y perfiles"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
]

[project.scripts]
md2docx = "md2docx.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
